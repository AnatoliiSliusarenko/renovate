<div class="partners center-block transparent">
	{% for partner in partners %}
  	<div>
  		<a href="{{partner.getUrl()}}">
  			<img src="{{ asset(partner.getDocument().getPath()) }}" alt="{{partner.getName()}}">
  		</a>
  	</div>
  	{% endfor %}
</div>